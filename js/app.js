// create a global module for the app where we can create routes, models, filters and directives
// App variable is accessible thruout application
// ngResource is an addtl module that defines functionality for creating resources (models) in angular
var App = angular.module('angular-app', ['ngResource']);
